# Notification Service Environment Variables

# Database Configuration
DATABASE_URL=postgresql+asyncpg://notification_user:notification_pass@localhost:5434/notification_db

# Service Configuration
SERVICE_NAME=notification-service
SERVICE_PORT=8005

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Optional: Email Provider (SendGrid, AWS SES, etc.)
# SENDGRID_API_KEY=your-sendgrid-api-key
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# Optional: SMS Provider (Twilio, AWS SNS, etc.)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_FROM_NUMBER=+1234567890

# Optional: Push Notification Provider (Firebase, APNs, etc.)
# FIREBASE_CREDENTIALS_PATH=/path/to/firebase-credentials.json
