apiVersion: v1
kind: Service
metadata:
  name: pg-donation
  labels:
    app: pg-donation
spec:
  ports:
    - port: 5432
      name: pg-donation
  clusterIP: None  # Headless service for stable DNS (needed by StatefulSet)
  selector:
    app: pg-donation
---
apiVersion: v1
kind: Service
metadata:
  name: pg-donation-access
  labels:
    app: pg-donation
spec:
  type: ClusterIP  # Use LoadBalancer only if you want external access (not recommended)
  ports:
    - port: 5432
      targetPort: 5432
      name: pg-donation
  selector:
    app: pg-donation
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pg-donation
  labels:
    app: pg-donation
spec:
  serviceName: pg-donation  # must match the headless service name
  replicas: 1
  selector:
    matchLabels:
      app: pg-donation
  template:
    metadata:
      labels:
        app: pg-donation
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: pg-donation
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: pg-donation
          env:
            - name: POSTGRES_DB
              value: donationdb
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password
          volumeMounts:
            - name: pg-donation-data
              mountPath: /var/lib/postgresql/data
              subPath: data  # optional, ensures nested directory for data
          resources:
            requests:
              cpu: 20m
              memory: 200Mi
            limits:
              cpu: 200m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: pg-donation-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: do-block-storage  # DigitalOcean's default storage class
        resources:
          requests:
            storage: 512Mi
