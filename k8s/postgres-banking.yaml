apiVersion: v1
kind: Service
metadata:
  name: pg-banking
  labels:
    app: pg-banking
spec:
  ports:
    - port: 5432
      name: pg-banking
  clusterIP: None  # Headless service for stable DNS (needed by StatefulSet)
  selector:
    app: pg-banking
---
apiVersion: v1
kind: Service
metadata:
  name: pg-banking-access
  labels:
    app: pg-banking
spec:
  type: ClusterIP  # Use LoadBalancer only if you want external access (not recommended)
  ports:
    - port: 5432
      targetPort: 5432
      name: pg-banking
  selector:
    app: pg-banking
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pg-banking
  labels:
    app: pg-banking
spec:
  serviceName: pg-banking  # must match the headless service name
  replicas: 1
  selector:
    matchLabels:
      app: pg-banking
  template:
    metadata:
      labels:
        app: pg-banking
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: pg-banking
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: pg-banking
          env:
            - name: POSTGRES_DB
              value: bankingdb
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password
          volumeMounts:
            - name: pg-banking-data
              mountPath: /var/lib/postgresql/data
              subPath: data  # optional, ensures nested directory for data
          resources:
            requests:
              cpu: 20m
              memory: 200Mi
            limits:
              cpu: 200m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: pg-banking-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: do-block-storage  # DigitalOcean's default storage class
        resources:
          requests:
            storage: 512Mi
